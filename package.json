{
  "private": true,
  "name": "custom-xml-sitemap-cloudflare-worker-javascript",
  "version": "1.0.0",
  "description": "Custom XML sitemap served using Cloudflare Workers - with a list of URLs within the repo as source.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Kevin-Ellen <45627930+Kevin-Ellen@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "wrangler": "^0.0.2"
  }
}
